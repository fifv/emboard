跨平台通用WebApp 管理面板

### Usage

1. Build
    ```
    make
    ```

2. push via adb then run
    ```
    make run
    ```


### Tech

Frontend

* TypeScript  - the language
* React + tailwind + daisyUI  - the UI framework

Backend

* Go - the language
* gin - the http framework